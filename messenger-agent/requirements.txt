# Web layer
fastapi>=0.115
uvicorn[standard]>=0.30

# Data validation
pydantic>=2.8

# Agent
langgraph>=1.0,<2
langchain-core>=1.0,<2
langchain-google-genai>=4,<5
# Memory across restarts. NOTE: needs psycopg[binary] below or you get
# "ImportError: no pq wrapper available" (verified the hard way).
langgraph-checkpoint-postgres>=3,<4
psycopg[binary]>=3.2

# Database (message log — separate concern from agent memory)
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29

# Cache / dedupe / debounce / locks
redis>=5

# HTTP client for the Send API
httpx>=0.27

python-dotenv>=1.0

# ── Dev / tests ──────────────────────────────────────────────
pytest>=8
pytest-asyncio>=0.23
fakeredis>=2.23          # Redis tests without a server
aiosqlite>=0.20          # repo tests without Postgres
ruff>=0.6
mypy>=1.11
pip-audit>=2.7
